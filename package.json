{
  "name": "project-calim-ke-front-end",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ./calim.ke run dev",
    "build": "npm --prefix ./calim.ke install && npm --prefix ./calim.ke run build",
    "preview": "npm --prefix ./calim.ke run preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}