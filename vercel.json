{
  "version": 2,
  "builds": [
    {
      "src": "calim.ke/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "calim.ke/dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
